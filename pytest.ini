[pytest]
addopts = -p no:warnings --strict-markers --import-mode=importlib -m "not integration"
asyncio_mode = auto
testpaths = tests
markers =
    unit: component-level tests focusing on domain/repository logic
    api: integration tests hitting the HTTP API surface
    asyncio: async tests
